export class PaginationHelpers {
    static PAGE_SIZE = 'page_size'
    static PAGE = 'page'
    static SORT_ORDER = 'sort_order'
    static SORT_BY = 'sort_by'
    static QUERY = 'query'
    static DEFAULT_PARAMS = [
        PaginationHelpers.PAGE_SIZE,
        PaginationHelpers.PAGE,
        PaginationHelpers.SORT_ORDER,
        PaginationHelpers.SORT_BY,
        PaginationHelpers.QUERY
    ]
}